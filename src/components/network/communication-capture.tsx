export function CommunicationCaptureNotice(){
 return <section className="card"><h2 className="section-title">Capture communication</h2>
  <p className="muted">The interaction database model is available, but a validated interaction mutation service has not yet been added to the application layer.</p>
  <p>Communication creation stays disabled here rather than writing directly from the browser and bypassing the service boundary.</p>
 </section>;
}
