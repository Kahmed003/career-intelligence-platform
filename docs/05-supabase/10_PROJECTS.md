Career OS — Projects Domain

Document ID: COS-DOM-PROJ-001Version: 1.0.0

Projects are the first vertical slice built on the shared platform.

Each project has:

one canonical object in public.objects

one domain row in public.projects

lifecycle inherited from the Object Registry

activity recorded in the Activity Ledger

relationships managed through the Relationship Graph

The projects.id value must always equal objects.id.
